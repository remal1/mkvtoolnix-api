// Reserved for phase 4: the public merge configuration facade.
// Phase 1–3 intentionally keeps the engine calls in c_api.cpp so that the
// first DLL can be validated with the smallest possible source change.
#include "common/common_pch.h"
#include "mkvtoolnix-api/merge.h"
